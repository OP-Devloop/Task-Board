const Header = () => {
  return (
    <header>
      <div>
        <h1>Task Board</h1>
        <p>A simple task board built with React, TypeScript, and components.</p>
      </div>
    </header>
  );
};

export default Header;